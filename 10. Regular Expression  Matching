import java.util.regex.*; 
class Solution {
    public boolean isMatch(String s, String p) {
      Pattern z = Pattern.compile(p);//. represents single character  
Matcher m = z.matcher(s);  
boolean b = m.matches();  
        return b;
    }
}
