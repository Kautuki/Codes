class Solution {
    public int missingNumber(int[] nums) {
      int n=nums.length;
        int t=n+1;
        int sum=(t*(t-1))/2;
        int s=0;
        for(int i=0;i<n;i++)
        {
            s=s+nums[i];
        }
        int p=sum-s;
        return p;
    }
}
